<Page
    x:Class="MoneyTracker2026.Views.DebtsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MoneyTracker2026.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Debts" 
                       FontSize="28" 
                       FontWeight="SemiBold" 
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <Button Content="Add Debt" 
                    Background="{StaticResource PrimaryBrush}"
                    Foreground="White"
                    HorizontalAlignment="Right"
                    Click="AddDebt_Click"/>
        </Grid>

        <!-- Method Toggle -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <RadioButton x:Name="SnowballMethod" Content="Snowball (Smallest First)" IsChecked="True" Checked="Method_Changed"/>
            <RadioButton x:Name="AvalancheMethod" Content="Avalanche (Highest Interest)" Margin="24,0,0,0" Checked="Method_Changed"/>
        </StackPanel>

        <!-- Summary Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total Debt" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"/>
                    <TextBlock x:Name="TotalDebtText" Text="£0.00" Foreground="{StaticResource ErrorBrush}" FontSize="28" FontWeight="Bold" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="20">
                <StackPanel>
                    <TextBlock Text="Monthly Payment" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"/>
                    <TextBlock x:Name="MonthlyPaymentText" Text="£0.00" Foreground="{StaticResource WarningBrush}" FontSize="28" FontWeight="Bold" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="20">
                <StackPanel>
                    <TextBlock Text="Debt Free By" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"/>
                    <TextBlock x:Name="DebtFreeText" Text="-" Foreground="{StaticResource SuccessBrush}" FontSize="28" FontWeight="Bold" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Debts List -->
        <Border Grid.Row="3" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="16">
            <ListView x:Name="DebtsList" Background="Transparent" BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding InterestRateText}" Foreground="{StaticResource TextTertiaryBrush}" FontSize="12"/>
                            </StackPanel>
                            
                            <TextBlock Grid.Column="1" Text="{Binding BalanceText}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding MinPaymentText}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="{Binding PayoffDateText}" Foreground="{StaticResource TextTertiaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
