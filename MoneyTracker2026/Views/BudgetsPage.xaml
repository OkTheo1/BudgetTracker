<Page
    x:Class="MoneyTracker2026.Views.BudgetsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MoneyTracker2026.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Budgets" 
                   FontSize="28" 
                   FontWeight="SemiBold" 
                   Foreground="{StaticResource TextPrimaryBrush}"
                   Margin="0,0,0,24"/>

        <!-- Month Selector -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="<" Click="PreviousMonth_Click" Width="40"/>
            <TextBlock x:Name="MonthYearText" 
                       Text="January 2026" 
                       FontSize="18" 
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="16,0"
                       VerticalAlignment="Center"/>
            <Button Content=">" Click="NextMonth_Click" Width="40"/>
            <Button Content="Add Budget" 
                    Background="{StaticResource PrimaryBrush}"
                    Foreground="White"
                    Margin="24,0,0,0"
                    Click="AddBudget_Click"/>
        </StackPanel>

        <!-- Budget Cards -->
        <GridView Grid.Row="2" x:Name="BudgetsGrid">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource CardBrush}" 
                            CornerRadius="12" 
                            Padding="20"
                            Width="250"
                            Margin="8">
                        <StackPanel>
                            <TextBlock Text="{Binding CategoryName}" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="16" 
                                       FontWeight="SemiBold"/>
                            
                            <ProgressBar Value="{Binding PercentUsed}" 
                                         Maximum="100" 
                                         Height="8" 
                                         Margin="0,12,0,8"
                                         Foreground="{StaticResource PrimaryBrush}"/>
                            
                            <Grid>
                                <TextBlock Text="{Binding SpentText}" 
                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding AmountText}" 
                                           Foreground="{StaticResource TextTertiaryBrush}"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
